node_modules/
workspace/
schedules.json
schedules.json.bak
schedules.json.corrupt*
config.json
config.json.bak
config.json.corrupt*
*.log
.DS_Store
__pycache__/
*.pyc
data/
backups/

# Docker 部署产生的：deploy.sh 会在仓库目录里建这两个
# wb-data/config.json 存着 API Key，.env 存着端口和域名——都不入 git
.env
wb-data/

# 第三方技能包：仓库不打包别人的代码，在应用内一键从上游装（技能页 → 推荐技能）
skills/ppt-master/
skills/frontend-design/
skills/canvas-design/
skills/theme-factory/
skills/brand-guidelines/
skills/webapp-testing/
skills/mcp-builder/
skills/follow-builders/

# 没有许可证、也查不到上游仓库的技能：来源不明的代码不往公开仓库里塞，留在本机自用
# （hongshuskill 还会用真 Chrome 操作小红书账号，替别人分发更不合适）
skills/hongshuskill/

# 猫叔本人的公众号管线：带 VPS 地址、公众号凭据路径、本机专属出网方案，纯私有自用
skills/wechat-pro/

# 用户自己装的 Agent Plugins 插件（plugins.js 是代码，plugins/ 是数据）
plugins/
.tmp/

# 评测跑批产物（每次跑的工作目录和分数明细）
eval/runs/

# 打包产物
dist/

# 从聊天窗口粘出来的临时文本，常带真实姓名/凭证，一律不入库
粘贴文本_*.txt

# 录 demo 的副产物：README 里嵌的是 GIF，mp4 只是录制时顺手产的，不入库占体积
docs/images/demo.mp4
