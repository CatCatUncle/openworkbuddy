# 复制成仓库根目录的 .env（deploy.sh 会自动做这件事），按需改。
# 这个文件不含任何密钥——模型 API Key 是在界面上填的，落在 wb-data/config.json 里。

# 数据放哪（config.json / 账号 / 会话 / 成果文件 / 技能 / 备份 全在这一个目录）
WB_HOME=./wb-data

# 宿主机上监听哪个地址、哪个端口
#   127.0.0.1 = 只有这台机器自己能连，外面得走反代（默认，也是推荐值）
#   0.0.0.0   = 直接对公网开。这个 agent 手里有 shell，想清楚再改。
WB_BIND=127.0.0.1
WB_PORT=3800

# 填了域名就能用自动 HTTPS：
#   1) 先把域名的 A 记录解析到这台服务器
#   2) WB_DOMAIN=buddy.example.com
#   3) docker compose --profile https up -d
# 不用 caddy（自己有 nginx）就留空。
WB_DOMAIN=

# 前面挂了几层反代（caddy / nginx / Cloudflare）。用 deploy.sh --domain 的话它会自己填 1。
# 为什么要填：挂了反代之后所有请求在服务端看来都来自代理那一个 IP，
# 于是「注册 5 次/15 分钟」「登录错 30 次/15 分钟」这两道防连打的闸，
# 会变成全公司互相锁死——第 6 个同事注册不了，有人连错几次密码大家一起进不去。
# 为什么不默认打开：没有反代却填了，等于谁伪造一行 X-Forwarded-For 就换一个新 IP，闸白装。
#   0 = 直连（默认）   1 = 前面就一层 caddy/nginx   2 = Cloudflare → 你的 nginx → 本应用
WB_TRUST_PROXY=0

TZ=Asia/Shanghai
