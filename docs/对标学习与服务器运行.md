# 对标学习与服务器运行

## 从 Mosoo / Multica 借鉴了什么

这两个项目的定位不同，但有四个值得吸收的共同点：

1. **一次执行有稳定身份**：任务、运行、事件、文件和后续追问不能散落在多处。OpenWorkBuddy 已有会话、成果子目录和本地 Trace，后续继续把它们用同一个 run/trace 关联起来。
2. **过程必须可观察**：工具调用、模型、耗时、失败原因、Token 和产物都能回放。Trace 页面按工具/模型分开显示，并保留原始输入输出。
3. **失败要能恢复**：重试、超时、取消、继续执行和版本快照应该是运行时能力，而不是靠用户重新描述。短剧画布下一阶段会把节点指纹、脏状态和下游重算接上。
4. **运行时与产品解耦**：桌面端可以跑本机 CLI，服务器可以跑容器/PM2；模型和 Agent 引擎通过配置切换，不把用户锁死在一个供应商。

OpenWorkBuddy 不直接复制它们的技术栈：当前项目坚持零前端构建、JavaScript 本地优先、文件交付和 Markdown Skill 扩展。这样更适合个人电脑、本地素材和希望读懂 Agent 主循环的贡献者。

## 能不能部署到服务器长期运行

可以。推荐服务器使用 Docker Compose：

```bash
git clone https://github.com/CatCatUncle/openworkbuddy.git
cd openworkbuddy
bash deploy.sh --domain buddy.example.com
```

它会使用 `restart: unless-stopped`，数据持久化在 `openworkbuddy-data`，健康检查访问 `/api/auth/state`，前面由 Caddy 负责 HTTPS。没有域名时也可以只绑定 `127.0.0.1`，再交给现有 Nginx 反代。也可以使用 PM2：

```bash
HOST=127.0.0.1 PORT=3800 OPENWORKBUDDY_HOME=/srv/openworkbuddy-data \
  pm2 start server.js --name openworkbuddy
pm2 save
```

服务器部署前必须关闭开放注册、创建管理员、启用安全中心的命令审批，并为数据目录做备份。不要把 3800 端口裸露到公网；Agent 具备文件和命令工具，公网部署必须有 HTTPS、登录和权限控制。

## 用真浏览器看网页（chrome_cdp）

`chrome_cdp` 让 Agent 开一个真 Chrome：打开网址、等它渲染完、点按钮、执行页面脚本、截图。
JS 撑起来的页面、WebGL/Canvas 画的东西，抓静态 HTML 是拿不到的，这条路能。

**不用你先启动什么。** 端口上没有调试服务时，它会自己拉起一个专用 Chrome：

- 端口由 Chrome 自己挑（`--remote-debugging-port=0`，再从 `DevToolsActivePort` 读回来），
  不跟 9222 上的任何东西抢。
- 配置目录单独一份，默认 `~/OpenWorkBuddy/chrome-cdp`，跟你日常那个浏览器两套登录态，互不打扰。
- 同一个配置目录上已经有一个活着的实例就直接接着用，不会每次开一个新窗口。

想让它接管**你自己**启的那个 Chrome，就传 `port`：

```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --remote-debugging-port=9222 --user-data-dir=/tmp/openworkbuddy-chrome
```

Linux 上把路径换成 `google-chrome`。找不到浏览器时，把可执行文件路径写进环境变量 `OWB_CHROME_PATH`。

其它几个开关：

| 环境变量 | 作用 |
| --- | --- |
| `OWB_CHROME_PATH` | 指定用哪个浏览器（Chrome / Chromium / Edge / Brave 都行） |
| `OWB_CDP_HEADLESS=1` | 不弹窗口。服务器上跑必须开；Linux 上没有 `DISPLAY` 时自动就是它 |
| `OWB_CDP_NO_LAUNCH=1` | 禁止自动拉起，只连已经开着的调试口（连不上就报错退出） |

无头模式下 WebGL 照样能跑（走 SwiftShader），所以三维卡片、Three.js、地图那类页面截出来是有画面的，
不是一句 `Error creating WebGL context`。

**连不上的时候先跑 `action: "status"`。** 它只看不碰，会直接告诉你端口上到底有没有 DevTools——
这一条很重要：端口能 telnet 通**不等于**调试口开着。Chrome 主进程本身就可能在 9222 上 listen，
但 `/json/version` 是空的，只看端口通不通会得出正好相反的结论。

服务器上的 OpenWorkBuddy 只能控制服务器上的 Chrome，不能穿透网络控制你个人电脑的 Chrome。不要把 CDP 端口通过公网、端口转发或反向代理暴露出去；如果以后需要远程浏览器，应单独做带身份认证、短时授权和审计的 Browser Worker。

---

参考：[Mosoo](https://github.com/langgenius/mosoo)、[Multica](https://github.com/multica-ai/multica)。
