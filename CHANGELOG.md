# 变更记录

一句话一条，最新在上面。README 里只留最近几条，全部在这儿。

每条对应的代码改动在 [commit 历史](https://github.com/CatCatUncle/openworkbuddy/commits/main)——
提交信息写的是「为什么改」，不是「改了哪个文件」，翻起来比这份清单细得多。

English: [CHANGELOG.en.md](CHANGELOG.en.md)

## 2026

- **09-19** 卡住的任务**自己会停下来了**，不再转到天亮。用户原话：「不要陷入长时间死循环这个问题给我解决下啊」。以前只有两档，都是「劝」：同一个调用连着三次拿到同样的结果就往历史里塞一句「换条路走」，连着五次拦一次不执行。模型不听就一路转到最大步数或最大运行时间——用户看到的是一句「已达最大运行时间，任务强制收尾」，还以为是活儿太多，回过头去把上限调大，下一趟转得更久、烧得更多。现在补上第三档**硬停**，五种卡法各认各的：同参同结果 6 次、一字不差的同一句报错 6 次（换参数也没用，说明参数根本不是变量）、连续失败 12 次、渠道都熔断了还连着调 4 次、以及 `A→B→A→B` 这种两步或三步一圈、每圈入参和结果都一样转满 4 圈——最后这种单看每个工具都不重复，只有连起来看才看得见。**这种停不自动续跑**（续跑第一件事就是把同样的圈再转一遍），停下来那句话也不再劝你去调大上限（上限再大它也只是多转几圈），而是点名让你去修它撞墙的那条路：渠道、文件还是命令。36 条断言里每一档都配一条**差一次就到的反向对照**——判据松一格，正在干活的任务就会被拦腰砍断，而误停比不停更伤人。
- **09-19** 资料库里的三处毛病一起修了。**音频和视频点开就能听能看**：用户原话「怎么没有办法预览啊」——一个 1006 KB 的 mp3 被判「文件太大，预览不动，请下载后本地打开」，因为它走的是读文本那条路，而文本上限是 512 KB。根子还在服务端：取文件那个接口**一律当附件发**，带着 `Content-Disposition: attachment`，`<audio>`/`<video>`/`<iframe>` 全都渲染不出来。现在默认内联发并带对 MIME，真要存盘的走 `?dl=1` 才加附件头。**二进制文件不再整包拉回来**：`.psd`/`.heic`/`.sqlite`/`.zip` 这些只发一个 HEAD 探它还在不在，直接说「二进制文件，下载后本地打开」；没后缀的看头几个字节认。**「出自任务」不再张冠李戴**：用户原话「这里说出自哪个任务也是错的位置啊！」——`任务_0915_对话_2/BGM_纯配乐.mp3` 被算到了 9-17 那个小红书任务头上，因为老写法认的是「第一个列出过这个文件的任务」，而后面那个任务确实也碰过它。现在先按文件自己所在的那个任务目录认，认不出来才退回去扫；两边都认不出就交白卷，不猜一个回去。**多了「所在位置」和「复制文件」两颗按钮**：用户原话「应该还有打开所在文件夹并且定位到对应位置的功能啊，还有能直接复制这个文件」——前者在访达里把那个文件选中，后者把文件本身放进剪贴板，粘到微信或 PPT 里是文件不是一条本机路径。这两颗只有本机那位（平台管理员）画得出来。
- **09-19** 「用真浏览器打开这个网页看一眼」这条路**修通了**。以前只有一个动作能用（列出有哪些标签页），导航、截图、跑 JS 全挂，还得你自己先开一个带 `--remote-debugging-port` 的 Chrome。挂的原因是四个毛病叠在一起：**9222 端口上蹲着一个不提供调试口的 Chrome**（端口通 ≠ 调试口开着，连接当场被拒）；握手里带了 `Origin` 头，而 **Chrome 111 起不再接受**，一律 403；**成功的那次也被当成失败**——校验的是「Switching Protocols」这个原因短语，而 Chrome 153 回的是「WebSocket Protocol Handshake」，状态码同样是 101；`evaluate` 的返回值**多剥了一层**，所以每次取回来的都是 `undefined`。现在它自己拉一个 Chrome：独立的用户数据目录、端口交给系统挑（`--remote-debugging-port=0` 再读 `DevToolsActivePort`），碰不到你正开着的那个窗口和登录态；已经有一个活着的就直接接管，不重复拉。无头档位下 WebGL 也真画得出来（`--use-angle=swiftshader`——老写法里那个 `--disable-gpu` 正是把 WebGL 弄死的元凶）。连不上的时候先跑 `action: "status"`，它只探不碰，直接告诉你是没装 Chrome、端口上是别的进程，还是被 `OWB_CDP_NO_LAUNCH` 按住了。新加 20 条断言，测试里那个假 Chrome 会对带 `Origin` 的握手回 403，这条回归以后跑不掉。
- **09-19** 它反问你的时候，**题面里点名的那个文件自己摊到右边**。用户原话：「然后问我这个文件里面展示的选哪个，然后这个文件也让我找半天不自己右边预览啊」——三版封面对比摆在一个 HTML 里，问题里也写了文件名，可你得自己去成果文件面板里把它翻出来。现在题面和选项说明里出现的文件名都认（《》里的、带路径的都算），**只认这一趟真写出来的那些**，所以不会弹一个「文件不存在」；问题下面还挂着这几份文件的按钮，随时点回去。跑完了才出产物那条路一个字没改——产出仍然不抢版面，只有「不看就答不上来」的提问才动右边那条。回放历史、别人的会话、窄窗都不开。
- **09-19** 成果文件面板顶上多了个**按名字找**。用户原话：「还有文件也比较难找到在这一堆文件里」——分组能解决「翻」，解决不了「我就要那一个」：一个任务跑下来几十个文件，得先展开对的文件夹再一行行扫。搜的是整条相对路径，所以文件夹名也算线索（打「0918」就把那天那个任务夹里的都捞出来）；空格分词、全都命中才算（「封面 html」= 名字里有封面、而且是 html）；结果不分组、直接摊平，每行标着它在哪个文件夹（同名的 `index.html` 一个任务能有好几份）。文件不到 8 个不摆这个框——一眼扫得完的时候它纯占地方。
- **09-19** 公众号封面**改成真生图**，不再拿 HTML 排版截个图充数。用户原话：「这封面你直接给生图，吸引观众有品味夸张」。根子在技能里：`wechat-article` 原来只写了尺寸（900×383、1:1），一个字没说画面该长什么样，于是每次都退回去拿排版糊一张。现在多了一步专门的封面，提示词按主体 / 风格 / 光与色 / 构图四行拼，三条硬规矩写死：**别让模型往图上写标题**（中文字必糊）、两个尺寸都交、一次出三个方向摆一个对比页让你挑——问的时候点名那个对比文件，网页端自己就摊开了。
- **09-19** 改文件那几步**能退回去了**：`write_file` / `edit_file` 落盘前先把改前那份留底（工作目录 `.history/`，按内容寻址，同一份内容只存一次），过程卡上点开就是这一步的 diff（加的绿、删的红），旁边一颗「回退到这步之前」——那一步和它之后动过的文件一起退回去，新建的删掉、改过的恢复；回退本身也留底，退错了再按一下「撤销回退」就回来。命令行是 `/rewind`，不带序号先列出能退的步。**「每步都问」档位下审批条上直接是这几行 diff**，批的是改动本身，不再是一个文件名——终端、网页、手机三处同一份。回退只认本会话的检查点，账本被手改塞进 `../../` 也写不出工作目录；30 天前的底自动清。
- **09-19** 修好了一条**把发版整个挡住**的红：v0.5.1 打了 tag，却一个安装包都没出来。CI 上挂的是前端那条「从资料库跳回那一轮，高亮得真画出来」——runner 上量出 `animation-name: "none"`。不是代码的事：GitHub 的 macOS runner **系统默认开着「减弱动态效果」**，而这一档下那条高亮动画本来就该是 none。于是同一份文件同一个 Chromium，开发机全绿、runner 全红，看着还像 CI 自己坏了。现在每个测试窗口都明说自己要哪一档（默认 no-preference，要测 reduce 的那两屏自己改成 reduce），结果不再跟着跑测试那台机器的辅助功能设置飘；两遍各加一条夹具自检，下次谁把这层摘了，报的是「这一遍不是 reduce 档」而不是一句莫名其妙的 `"none"`。本机拿 `--force-prefers-reduced-motion` 把 runner 的状态复现了一遍：改之前照样红，改之后 54 屏全过。
- **09-19** 被掐掉时那半句「任务强制收尾」现在**说得出下一步**。用户原话：「已达最大运行时间，任务强制收尾怎么回事啊」——老措辞只有一句「如需继续，可提高设置中的上限」：上限在哪一页没说，默认就是 30 分钟也没说，更没提还有「自动续跑轮数」这个开关（默认 0，所以撞了墙什么都不会自己接着跑）。现在这句点名「设置 → 执行上限」、点名「自动续跑轮数」，并把进度档的位置（工作目录的 `PROGRESS.md`）一起给出来，看到的人不用再回过头来问。**手动停止不再被当成撞上限**：人自己按的停，还劝他去调大上限就是答非所问，那一条只说怎么接着做——Web 过程条和命令行里那句 `▲ …，任务强制收尾` 同样不再跟在「已手动停止」后面。措辞也从两份收成一份：内置循环和本机 CLI 引擎以前各拼各的，一边改一边不改就会在 Web 和 IM 上说出两种话，现在都叫 `stopNotice()`，谁再手抄一遍测试当场红。
- **09-19** 定时任务的每一趟执行，现在都留下**一整段能点开回放的会话**。自动化页每条任务底下是「运行记录」，每条记录后面挂着「看执行过程」——点进去就是那一趟调了哪些工具、每一步返回了什么、最后为什么是这个结论，跟手动对话完全同构，走的也是同一个 `openSession`，没有第二套回放。用户原话：「我定时任务怎么没看到具体的执行过程啊」「我想要看到每次具体的运行记录啊」。以前这儿是光秃秃一句 `runTask({ history })`：不给 emit 就没有事件，不给 sessionId 就没有会话，一整趟执行只在记录上留下一句**被截到 500 字**的结果，想知道它卡在哪一步一点痕迹都查不到。三处细节都是照着「背着人跑」这件事定的：**定时会话不进侧栏**（一天几十条 cron 会把真正的对话整个挤下去，它们的正经入口是运行记录那一栏）；**宠物不为 cron 动表情**（背着人跑的任务让桌面宠物手舞足蹈，会盖掉用户手头正在看的那件事）；**记录删了会话跟着删**（记录 300 条封顶、任务删了记录也跟着删，可盘上的会话文件没人碰——用得越久越多，而且全是没有入口的孤儿）。录像不许影响执行：起录像失败就当没这回事继续跑，会话 id 先落盘再开跑，任务跑一半崩了那半段过程也还找得回来。
- **09-19** 能排**「只跑一次」**的定时任务了。用户说「五分钟后叫我去准备面试」，以前排出来的是一条**每天 14:00** 的 cron——他的原话是「你在搞什么啊」。根因不在模型：cron 五个字段里压根没有「一次」这个概念，模型手上只有这一个工具，只能拿「每天这个点」去近似「这个点」，于是一条提醒变成了一条每天都要响的闹钟。现在认三种写法：`+5m` / `+2h` / `+1d`（**这条是主力**——系统提示词里的当前时间只精确到「几点左右」，模型算不出「五分钟后」的绝对时刻，硬让它算只会算歪，不如让它照抄用户说的那个量）、`14:05`（今天这个钟点，过了就顺延到明天）、`2026-09-19 14:05` 或完整 ISO。**认不出来一律抛错，绝不猜一个时刻回去**：排期是要弹给用户点头的，猜错的那一下他点的是「同意」。响过之后状态是「已完成」不是「已暂停」（暂停的言下之意是「还会再跑，等你开」），记录留着——人还要回头看「那条提醒到底响没响」。关机错过了照样补，这正是一次性提醒最该补的场合（合上笔记本去开会，回来该看见它响过），但隔了一天的「五分钟后叫我」再响就是骚扰，超过补跑窗口就只记一句「错过了，晚了 N 分钟」。关掉自己这一下是**同步**做在放行之前的：fire 是异步的，等任务跑完再关，中间每一分钟的 tick 都会看见它还开着、时刻还是过去时——一条五分钟的提醒会连着响到你手动关掉。
- **09-19** 资料库里**点一个文件真的能看见内容了**。用户原话：「在资料库怎么没有办法打开文件啊！」——内容确确实实渲染进了右栏，只是那块板子还挂着 `display:none`（右栏是按需出现的，没选东西时 `data-prev=off`），于是屏幕上什么都不发生。之所以一直没人发现：随便点一下筛选器就会走整页重画，那一路是照选中项重算这个属性的，于是又能看了——看起来像「偶尔抽风」，其实是每次进这一页的**第一下必挂**。顺手把预览格式补齐：Word / Excel / PPT / CSV / TSV / zip 在这一页也能直接看了，画法和样式跟对话页共用同一套，不抄第二份；`.xlsx` 多张表的标签点得动（一个表格文件常常好几张表，点不动就只看得见第一张）；CSV 按 RFC4180 认引号、自己判分隔符是逗号分号还是制表符（欧洲导出的表用分号，按逗号拆会拆成一整列）。Office 97-2003 那三种是 OLE 二进制不是压缩包，拆不开就明说是格式的事、并给一条能走通的路（另存为新格式），比让它掉进「这个文件不是文本」强得多。
- **09-19** 输入框左边那个 **＋，现在一个入口装下六件常干的事**：添加文件、切模式、挑专家、挑技能、看**模型此刻手上到底有哪些工具**、开关已经配好的连接器。用户原话：「我这里也有 ＋ 能看到各种工具啥的啊，还有管理已经设置好的连接器这些啊」。在这之前这六件事散在六个地方：上传是一枚回形针，模式在快捷栏最右，专家和技能要跳到另一页（回来时对话已经翻页了），而「有哪些工具」——**界面上根本没有**，只能问它一次、等它答「我没配发信通道」。一级只有六行字，二级从右边飞出来一块 300px 的板子摆清单，所以一级不会被二级撑变形、来回切也不跳宽度。连接器那一栏能**直接关掉某一台**：关 ≠ 删，配置密钥参数全留着，只是这一轮不去连它、工具表里也不出现——留着定义模型就会先想一个用它的方案、调一次、吃一条「连不上」、再重想，白烧一轮。关掉的那几台在界面上跟「连不上」分得清（少一个字段，用户关掉一台看到的是一张灰卡片，跟连接失败长得一模一样），「授权没了」也和「连不上」分开说（前者要你现在就去换 Key，后者多半过会儿自己就好）。连接器是整台机器一份的，开关归平台管理员，跟权限档位同一个判据。
- **09-19** **撞不通的模型不再一趟一趟去撞**。用户原话：「一些不能用的模型，不要一直去用啊！」「我这个视觉模型现在都用不了就不要用了啊，老是卡住干嘛」。五路媒体工具（看图 / 生图 / 生视频 / 配音 / 转文字）现在统一穿过同一层派发：401 / 402 / 403 / 404 这种「不换东西就永远不会好」的错一次就停 30 分钟，超时连不上这种攒够 3 次停 5 分钟，400 / 422 是参数的事不算渠道的账。为什么记在派发这一层而不是五个函数各自的开头：那五个函数里散着三十多个 `return {isError:true}`，一个一个去记账早晚漏掉一条，而漏掉的那条恰好就是撞得最凶的那条。指纹按「这一路 + 地址 + 型号 + Key 的哈希前 8 位」算，所以换把 Key、换个型号立刻重新可用，不用等冷却。光有这道闸还不够：模型该转的圈还是照转，用户看到的是 trace 里四十条一模一样的「看图 · 失败」——所以同一个工具被熔断闸拦到**第二次**，本轮就不再执行它了（第一次拦下时模型还没读到那句话，它有权换个型号点名另一条渠道再试一次；读过一次还撞就不是判断问题了）。熔断在后台默默生效就行，但「为什么它突然不给我看图了」必须能看见：设置 → 模型 页顶上一条黄条摊开说是哪一路、还剩多久、为什么。**在设置里动过渠道配置就立刻清空熔断记录**——你改完还得干等半小时，然后来问「我都改好了怎么还不动」。
- **09-19** 看图模型的下拉**按渠道自己报的模态分组**，不再按名字猜。拿 OpenRouter 当天那 446 个型号实测：真能接图的有 263 个，按名字只认得出 134 个——用户自己配的 `z-ai/glm-5.3-flash` 名字里一个 vl / vision 都没有，于是被扔进了「其它模型」；另一头 `gpt-5-image`、`gemini-3-pro-image` 这些**出图**的名字里带 image，照样在看图的下拉里排着队等人选。用户原话：「然后有些似乎是生图模型怎么给我放到看图模型里面去了啊…」——选中一个出图的拿去看图，上游要么报一串看不懂的参数错，要么真去画了一张图再超时，界面上只剩「卡住」。好在 `/models` 这一层越来越多家会报 `architecture.input_modalities` / `output_modalities`（OpenRouter、new-api、one-api 都透传），那是渠道自己说的，比猜准、也比我们维护一张永远追不上的型号表准：报了就信它并标成「确定」，没报才退回按名字猜并标成「不确定」。前端拿这一格决定**敢不敢把一条拦在「看图」那一组外面**——猜出来的不敢拦，人家自己说的才敢。进出都只有文字的那批是**确定**哪一路都不是，说死了才拦得住 `gpt-5.x-codex` 混进看图那一组；既能接图又能出图的一律先认「产出」那一路（把 `gemini-3-pro-image` 配在看图上是纯纯的浪费和卡顿，它的正业是画图）。
- **09-19** 同名渠道会**自己编号**。同一家配两条（一条自己的 Key 一条公司的、或者一条正式一条备用）是常事，可下拉里两行字一模一样，选哪个全靠猜；改错了那条还以为改的是另一条。现在重名的按配置顺序标上「①②③」，只有重名才标——没重名的不该凭空多个符号出来。
- **09-19** 「添加模型」那张表单**不再被异步重画冲没**。精选目录是现拉的，拉回来会把这一屏再画一遍：人点了「添加看图模型」、正在下拉里翻型号，目录恰好这时到了——整张卡重画，表单连同刚选好的那一行一起没了，按钮看着还在，点下去又是空的。用户原话：「怎么点击添加都没办法添加在搞什么」。现在表单开着就不重画：重画本来只为了「把目录补上」这点好处，不值得拿人填了一半的东西去换。同一句话还有另一半：保存失败时那行提示**穿着成功的衣服**——绿色、13px、挂在一整屏表单的最底下、上面还压着五张折叠卡，用户看到的是一行绿色小字，跟「已保存」长得一模一样。其实每次都报了错，只是那个错藏在屏幕外面。现在失败一律另外再喊一嗓子，不指望那行小字。
- **09-19** 换过的**桌面宠物重开还在**。用户原话：「这个宠物我之前换了的，然后重新打开又是默认的猫猫宠物了」。开机时那份配置是手抄五个字段传过去的，**偏偏漏掉了 sprite**：于是选了精灵图宠物的人，`character` 传成了 `"sprite"`、`sprite` 却是空字符串，拿空 id 去查自然找不着，最后那行兜底静静地落回 `"cat"`。同一份名单还漏了「跑完通知」和「四处走动」，缩放的兜底值也跟宠物自己的默认值对不上（那边是 2，这里写死 1）——三处都是同一个病：手抄一份字段清单，以后加字段的人不会想到回来改它。现在整份摊过去，只把「启用」单独钉死成必须显式为 true（没配过就不该有宠物），其余交给宠物自己的默认值，以后加字段不用再动这里。
- **09-19** **粘进输入框的图不再算成上一轮的产出**。用户原话：「我在一个对话里面，对话还在执行的时候，我复制一个图片来问问题结果，之前执行的成果区出现了我在问问题的图片啊」。根子在上传那一步把文件直接落进了这条会话的成果文件夹——那一步是对的，素材和成果待在一起，否则工作目录根下越堆越乱（真实数据里躺过 22 个）。可「本回合产出」的判据是「在我的文件夹里 + 修改时间在开跑之后」，这张图两条全占：它确实是刚出现的新文件，只是写它的人是用户不是 agent。**谁写的这件事只有落盘那一刻知道**，事后从盘上看一个文件是看不出来的，所以只能在那一刻记一笔。记的是「名字 + 那一刻的修改时间」而不是只记名字：agent 后来真把这张图改写了（抠图、压缩、换格式），时间一变就不再算输入——那时候它确实变成了产出，本来就该出现在卡片里。
- **09-19** 安全档位**真的落到本机 CLI 的命令行上**了。这两个 CLI 自带工具、自带循环，它们写文件、跑命令**不经过**本项目的安全中心（只有从 MCP 桥回流的那批才走那道闸）。以前 claude 这条路硬写死 `acceptEdits`、codex 这条路硬写死 `workspace-write`，于是用户在设置页把档位调到「只看不动」、切到本机引擎照样随便改文件——界面上那颗开关是个摆设，而且是最不能当摆设的那一颗。现在四个档位一路翻译下去：只看不动 → claude `plan` / codex `read-only`，问一下 → `default` / `read-only`，全自动 → `acceptEdits` / `workspace-write`，不拦 → `bypassPermissions` / `workspace-write`。「这类命令要问我一下」那张名单在这条路上**问不着**（`-p` 非交互，没有审批通道），所以只剩不给用：名单上每条取第一个词收成 `--disallowed-tools Bash(sudo:*)` 这种（名单写的是前缀如 `diskutil erase`，而 CLI 那边的匹配单位是可执行文件名），删除保护是另一颗开关，也跟着收 `Bash(rm:*)`。「只看不动」还额外**不把借出去的命令行入口放进白名单**——那是条后门，模型从那儿照样能写文件。手填的 `engine_options` 仍然最大，不配 guard 时老行为一字节不变。档位收紧了会在运行页明说一句：不说的话，用户看到的是「它怎么什么都不肯干」，而真正的原因在另一个页面上的一颗开关里，隔着两层根本联系不起来。
- **09-19** `@` 提到文件的那张菜单，**一屏十二行终于认得出哪行是哪个文件**了。以前名字显示的是完整相对路径，可任务目录名本身就有二十几个字，于是十二行全叫「任务_0918_帮我做一个『openwor…」；而右边那句说明十二行一模一样（「工作空间文件」），等于白占一行。现在名字只显示最后一段、目录名让给说明那一行，插进正文的仍然是完整相对路径。名字自己包一层元素才拿得到省略号（裸文本节点在网格里是匿名盒子，长文件名只能硬折），图标一律给（取不到就用通用那个，否则有的行有图标有的没有，名字的左边缘会错开一截）。
- **09-18** 资料库里点一份产出，**直接落到写出它的那段对话上**，不是把整条对话从头摆出来。文件行后面多了个小气泡图标（鼠标扫过那一行才亮），出自哪次任务那一行也标着「第 N 轮」。原来点过去只是打开对话、一脚滚到最底下——一次跑了十几轮的任务，人还得自己往回翻，等于这一跳什么忙都没帮上。回合号是数**对话记录里的 user 条目**算出来的，不是拿数组下标充数（回放时一条 user 起一个回合，下标里还夹着 assistant，差着将近一倍，跳过去正好落在一段跟这份文件毫无关系的话上）。同一份文件改过好几轮就认头一轮——你点它是想看「这东西怎么来的」，那句话在第一次写出它的那一段里。**跳不过去就认输**：老会话根本没记过回合、记录也可能被裁过，这时候服务端一个字都不编，界面干脆不画这个按钮，照旧把对话滚到底。落点会亮一下再退（开了「减弱动态效果」就不淡入淡出，底色直接摆上）——不标记的话人只知道「页面动了一下」，不知道该看哪儿。
- **09-18** **能引用某一条回复来追问了**。回复底下的操作条多了个「引用」：按下去那条回复的正文以 `>` 引用块的形式落进输入框，你接着在下面写自己的话。**先选中一段就只引那一段**——一条回复常常好几屏，整段引过去等于什么都没指。为什么是塞进输入框而不是挂一枚标签：引用的内容**要能改**，真实用法几乎都是「它这段里有一句不对」，人会把那句留下、其余删掉。只取渲染后的正文，过程卡片、按钮条、token 统计一个字不带；输入框里写了一半的话不会被冲掉，空一行接在后面；同一段连按两下不会引进来两遍（会说一声「已经在输入框里了」）；超过 400 字截断并打省略号——再长就不是引用而是复述，模型也会被那一大坨带偏。
- **09-18** 补上三个**一直在画空框框的图标**：自动化列表的「上次跑」、安全设置页的「技能与连接器体检」、画布的「最近一次生成」。`<use href="#i-打错的名字">` 不报错也不警告，浏览器安安静静画一个跟正常图标一样大的空格子，只有盯着那一格看的人才发现里头没东西。顺手加了道闸：界面上 379 处写死的图标名，每一处都对着雪碧图核一遍（两个页面各有各的一张，分别核），核不了的那 78 处变量名如实报个数，不假装全覆盖了。
- **09-18** 接上了 **Jev 判断模型**（TypeSafe System One），它不写字，只回选项 / 分数 / 概率，外加一个「有多确定」。它没挂进「模型」那个下拉——对话模型吃一段话吐一段话，它吃一段材料加一道**有类型的**题，挂上去每一趟都是 400，所以走的是单独一条路：命令行 `openworkbuddy jev`、接口 `/api/decide`、设置页里加一条渠道点「测一下」（那个按钮认得出它，不会拿对话接口去 ping 一个必然的 400）。**配过 OpenRouter 的人什么都不用填**，原来那把 Key 直接能用。第一处真用上它的地方是**目标模式的验收**：原来让对话模型对着几条标准吐一段 `{"results":[…]}`，那是拿会写字的东西模拟一张表格，它偶尔就不按格式回话、整轮打勾原地不动；现在每条标准是一道是非题，格式在类型上就跑不掉。更要紧的是多了一格确定度——**过 70% 才打勾，没过线的留着不打并在卡上写清是「拿不准」而不是「没干」**，因为这张卡最骗人的地方就是打了勾的人就不再看了。没配这个模型的机器上安静退回原来那条路，行为一个字节不差。额度按**题数**记不按请求数记（一次能塞 32 道题，按请求记那道闸等于没有），输出 token 不要钱，一趟三道题约两万分之一美金。
- **09-18** 侧栏的**任务历史能搜正文了**，还能按意思搜。原来那个放大镜只筛标题，可标题是任务跑完自动起的，用户从没读过一眼——他记得的是自己当时打的那句话（「把这个 csv 里重复的行挑出来」），或者最后拿到的那个文件名（「清洗结果.xlsx」）。按标题筛这两种记法一条都找不着，只能一条条点开看，点到第五条就放弃了。现在按三种命中找，**每条都标清楚是靠哪一种找到的**：标题 / 产出文件 / 对话里 / 词面接近 / 意思相近。不标的话，语义命中那几条看起来就是凭空冒出来的不相干任务，人只会觉得搜索坏了。打字时本地立刻筛标题、不等网络（等的话列表会空一下，而空列表看起来跟「没搜到」一模一样），300ms 后服务端那一路跟上并接管；搜挂了就退回本地筛标题**并说明是挂了**，不拿「没搜到」糊过去——那是两件事。没配嵌入渠道时结果里明说「意思相近这一路这次没走」，不悄悄降级。命令行的 `/resume` 同样吃这一套：`/resume 重复的行` 接得上标题里没这几个字的那条，选单里只靠正文对上的行会标一句「对话里」。搜索照样过归属过滤——**侧栏看不见的会话也搜不到，搜索不是绕过归属的后门**。
- **09-18** 选择器顶上补了个**搜索框**，而且一直摆着。原来是打了字才在标题后面回显一句「搜「周报」」——于是没打字的人根本不知道这儿能搜；「打字搜」那三个字是写在脚注里的，夹在一串按键提示中间，没人会去读一行提示。现在框在第一眼就杵在那儿，空着的时候写明筛的是什么（`/resume` 是标题 / 会话 id / 来源，`/model` 是型号名和厂商），打进去的字原样回显、光标跟着走。**搜空了框里也留着刚打的词**——一清空人就不知道该退格删什么。脚注顺手把「打字搜」删了：框已经在眼前，同一句话说两遍只是把按键提示挤长。
- **09-18** 终端里接回之前那段对话，从「数序号」变成「挑」。`/resume` 不带参数弹一个选择器：↑↓ 挑、打字实时搜（标题、会话 id、来源都在搜索范围里）、回车接上、Esc 算了，桌面端开的那些也在同一张单子里，每行标着来源、聊了几轮、多久以前。原来是印一张带序号的表让人对着敲——序号得用眼睛数，数错一位接的就是**另一条会话**，而且这种错要到下一句话答非所问才发现；会话一多还会翻屏，第 13 条根本不在表上。`/model` 走的是同一个选择器，两条命令共用一套交互，省得长出两种。列表比屏幕长就自己滚，选中的那条始终留在窗口里——露不出来的话人按着按着就不知道按到哪儿了。**管道里跑、或者这台终端画不了覆盖式界面，两条都自动退回老样子**（印一张表、敲序号），跟 `/` 菜单共用同一个「这台终端不认」的开关：画花过一次，两边一起退，少一半交互也比把人的终端搅烂强。
- **09-18** 命令行补上三条一直缺的命令。`/compact` 手动把前面聊过的压成一段摘要腾地方——**手动敲的这一下不看阈值、也不看「关了自动压缩」那个设置**，那个设置管的是「别自作主张」，不是「不许我自己压」；原文照样归档，压完报一句「12 条并成 1 条，上下文 100k → 40k，省了 60%」，没压成就直说没压，不拿「还没到阈值」当交代。`/diff` 列这个会话动过哪些文件，**以工具调用为准、不听模型自述**——它说改了而没真调 `write_file` 的情况是存在的，听自述等于替它圆谎；文件后来被删了也照列并标「没了」，悄悄不显示是另一种圆谎。工作目录是 git 仓库的话顺带出 `git diff --stat`，不是的话明说「给不了逐行 diff」，免得人以为 diff 坏了。`/mcp` 报连接器接上了没有、各自带了几个工具，**没接上的要说清楚卡在哪儿**（token 过期就写 token 过期）——只打个叉的话，人只会反复重启，重启一百次也还是 token 过期。
- **09-18** `/init` 让它把当前工作目录摸一遍，写一份 `AGENTS.md` 放在根下：项目是干什么的、怎么跑怎么测、哪几个目录是主干、有哪些既定规矩。这份文件桌面端和命令行每趟活儿开跑前都会读，所以它准不准直接决定后面几十趟顺不顺。**它不是自己动手写文件，是把这句话交回去当一趟正常的活儿跑**——权限档、改文件前的确认、`/diff` 里的记录一个都不少；按一下命令盘上就凭空多个文件、中间什么都没问过，是这类命令最容易犯的错。措辞上也拧了两处：命令照抄真在 `package.json` / 文档里看到的那几条（不然它会顺手编一条 `npm test` 出来，而这个项目可能根本没有），规矩只写找得到依据的、找不到就不写，别拿通用建议凑满十条。目录里已经有 `AGENTS.md` 或 `CLAUDE.md` 的话先读完再增补，**不整篇覆盖**——那份八成是人手写的。
- **09-18** 两个键位，省得为了换个档去敲命令。`Shift+Tab` 循环切这一趟的权限档（只看不动 → 每步都问 → 自动改文件 → 转回来），每按一下当场把切到哪档、那档意味着什么打出来，而且**只管这一趟，配置文件一个字不动**。**这个圈里没有「全自动」**：那个键就挨着 Tab，误碰一下就把命令确认也关了，而人正盯着自己那半行字，根本不会去读屏幕上多出来的一行——要全自动得明着敲 `/perm full`。`Esc` 连按两下，把这个会话里问过的话摆出来挑一条，原样放回输入行改了重问。它**只管放回去，不回卷历史**：真把已经跑过的那几轮删掉，等于把模型做过的事悄悄抹了，而人看不见抹了什么；要重来开 `/new`，要接回去用 `/resume`，两条路都在明面上。顺带治好一处：`/d` 不再是谁的简写——`/diff` 和 `/drop` 都以 d 开头，收成简写就等于替人在两条里挑了一条，而他只打了一个字母。
- **09-17** 任务跑完，顺手问一句要不要把中间文件清掉。一趟短片任务拆出几百张帧图、一段配乐分出四条音轨、一次打包吐出一个 `node_modules`——成片交出去之后这些还躺在工作区里，一个月下来能占掉几个 G，而没人会专门去翻。现在任务收尾时多一张卡片：「这一趟顺手造了 N 个中间文件，占 XX」，按来路分成五组（抽出来的帧、分离出来的音轨、打包产物、草稿和临时件、一次性脚本），每组写清楚**为什么敢删**（成片还在、原音频还在、`package.json` 还在、这是跑一次就没用的脚本），勾好按一下就清，「先留着」什么都不做。首页「把事情交给我」那一屏也钉了同一个入口，上面直接写着此刻能腾出多少——这么高频的事得放在手边，藏在设置里的清理功能等于没有。**面板分两半是故意的**：上半是我敢说它是中间物的（可删，每条带理由），下半是「地方到底花在哪了」——按任务列出占用，只报数不评判。真正压着盘的往往是三周前那个视频任务的成片，规则永远不会去动它，但你得先看得见它才谈得上自己决定；那半边每行都能「只整理这个任务」或者直接在访达里打开。**红线**：`apply()` 只认这一轮 `plan()` 自己提出来的那份名单，名单外的路径一律拒——就算有人把 `../../users.json`、`/etc/hosts` 直接塞进请求也一样，因为这是个能删文件的接口，白名单之外根本不该存在第二条路。交付物（成片、报告、`交付说明.md`）在任何一条规则里都不参与；`package.json` 还在的目录才算打包产物；源码树里的 `.py` 不算一次性脚本；同一批 wav 只报一次，不会同一个文件被两条规则各算一遍。删就是真删、不进回收站——挪进 `.trash` 只是把同样的字节换个地方占着，而用户要的是空间回来；删掉的每一条都写进审计日志，事后查得到是谁在什么时候清的
- **09-17** 资料库不再拿一屏「点开告诉你文件不存在」的行糊弄人，而且两种原因分开治。**升级那次被挪走的，跟着搬家找回来**：`migrate.js` 会把工作区根目录下躺过一天的散文件收进 `以前的文件_日期/`，搬完之后资料库还拿当初记下的裸文件名去查，一整批好端端躺在盘上的文件被判成「已经不在」——它只是往下挪了一层。现在按新地址报出去，点开真能打开，顺手认领掉，免得同一个文件在「未归属」里再出现一遍。不做全盘按文件名搜是故意的：两个任务目录里各有一个 `cover.png`，搜回来很可能是另一份，而「打开的不是我要的那张图」比「说它不在了」更难发现；`at` 只在当前工作目录下认，因为它是要拿去当地址用的，在别的根里找到一个同名文件却给不出能打开的地址，比说它不在了还糟。**真删掉的那批，默认不摆出来**：一次视频任务拆出几百张帧图、分析完自己清掉，这些行摆在那儿，每一行都是一句「这儿有个文件」，点开却说没有，那一趟点击是纯亏的；整组都没了的任务整组不露面，留个只能点出「文件不存在」的空壳没有任何用处。但不是悄悄吞掉——底下写着还有几个没显示，一点就全回来（划掉并标「已不在」），这个选择记在 localStorage 里，不用每次重按
- **09-17** 公司买的各家 API 收到一个口子上，员工和别的程序都不再碰真 Key。上游 Key 在后台填一次，发出去的是一把 `sk-` 开头的虚拟 Key，对外是标准 OpenAI 协议——别人的代码一行不用改，`base_url` 换成你的地址就能跑。**不只是对话**：`/v1/chat/completions`、`/v1/embeddings`、`/v1/images/generations`、`/v1/videos/generations`、`/v1/audio/speech`、`/v1/audio/transcriptions`、`/v1/search` 七条路都在同一道闸子下面——公司买的从来不只是对话那一份，而漏在闸子外面的那几份恰好最贵（视频是按秒算的）。每把 Key 单独勾能力：只给生图的那把去调语音当场 401，而且 `/v1/models` 里根本不列对话型号——列出来的每个 id 都是一句「你可以叫它」的承诺，让它叫过去只拿到 401，对面的程序员会先去翻自己的代码，翻半天才发现问题出在一个能力开关上。生图生视频的产物落在 `/v1/files/<id>/content`：24 小时过期、过期即删，别的公司拿这个 id 去取返回 **404 而不是 403**——403 等于承认「这个 id 是存在的」。真 Key 只在服务器内存里拼进上游请求，日志和回包里都不出现；虚拟 Key 存的是哈希，界面上只看得到后四位，丢了只能重发一把，找不回来

- **09-17** 中转站的账按「这家怎么卖」算，额度是硬闸不是报表。对话按 token 计（元 / 百万 token，内置一份截到 2026-09-17 的价目，美元按 7.1 折），其余四种各按各的单位：生图按张、生视频按秒、语音合成按千字符、转写按分钟——统统折成 token 记的话账面会好看得离谱，而月底的账单不会跟着好看。单价三层覆盖：内置 → 后台改的 → 某条渠道单独改的；后台那张表按「能力 + 型号」改一条，存下去立刻生效，每一行标着这个价是内置的还是你改的，审计员看得见、改不动（能改就等于这一页上所有的账都能被改成 0 元）。算不出价的（比如刚上线的型号）单独记成「不知道多少钱」一列，**不记 0**——记成 0 的那一刻这笔钱就永远找不回来了。额度是先扣后结：发出去之前按预估扣住，回来之后按实际结算，超了当场返 402 `insufficient_quota`。三级上限：组织 / 部门模板 / 单把 Key，填 0 是不限。**员工在界面上自己点的、定时任务跑的，花的是同一笔预算**——以前只有对外那条路过闸，漏的恰好是能一直点「重试」的那半边。超额的提示说清是谁的额度满了：「整个组织这个月的预算用完了，不是你一个人的：上限 2.00 元，已用 2.10 元」——不说清楚的话，人会以为是自己手气不好，回去再点第二次、第三次

- **09-17** 工作区里点开 `.mjs`、`.py` 这类源码，现在按代码画：左边一栏行号（横着拉的时候钉在原地）、关键字/字符串/数字/注释分五色，而且**不折行**。原来它走的是给日志和纯文本调的那条兜底 `<pre>`——`white-space: pre-wrap` 加 `overflow-wrap: anywhere`，长 URL 不把面板撑宽是对的，可同一条规矩落到代码上就是从**任意位置**断字：`fun` 留在上一行、`ction` 掉到下一行，看着「像乱码」——字都是对的，断点全是错的。前端打包产物更惨：rolldown / esbuild 吐出来的 `.mjs` 常常整份文件就一行几万字符，糊成一坨。所以认出压缩产物（最长一行超过 400 字符）的时候默认先替你排好版——按 `{` `}` `;` 断行、跟着括号深度缩进，横幅上写着最长一行有多少字符，旁边一个「看原文」随时切回去。排版只加换行和缩进，一个字符都不改，也不回写文件。断行这件事**故意没用 AST**：打包器的产物什么怪语法都有，解析器一遇到不认识的就整份罢工，而一个只认字符串、注释、正则的字符扫描器最差也就是断得难看，不会丢内容。这三样是必须认的——`var q="}}}"` 里的花括号不是结构，`/[/{};]+/g` 里的斜杠不是注释，`for(let i=0;i<3;i++)` 里的分号不是句末，每一样都有单独的测试盯着。着色按语言分六档（C 系 / `#` 注释系 / JSON / CSS / 标签系 / SQL），颜色是照深色底 `#16181f` 量过对比度的（注释那档从 3.1 提到 5.2——注释是拿来读的，不是拿来隐身的）。超过 5000 行只画前 5000 行，横幅上直说（文件本身没被动过，下载下来是完整的）

- **09-17** 装别人的技能之前先体检一遍。技能正文本身就是写给 agent 看的指令，而 agent 手里有 shell——一个链接装进来，它下一次干活就照着做。现在装之前扫一遍：目录、GitHub 上单个 `.md`、界面上自己粘一段，三条路都过同一道闸。分三档：**能过**照装；**有话说**（外网地址、装依赖、读环境变量）把原文摘出来给你看，再点一次确认；**别装**（`curl | bash`、读 `id_rsa`、翻 `~/.aws`、`rm -rf ~`、反弹 shell 之类共 10 条）直接拦下，只有平台管理员能强行放行，放行这一下会记进日志、也写进技能目录里的 `.install.json`——从哪个仓库哪个 commit 装的、谁放的行、当时扫出什么，都在里面。不算分：34 条规则里只有 10 条会拦，剩下 24 条一律只提醒，因为「7.5 分要不要装」没人答得上来。看的是组合不是关键词：`printenv` 单独出现不算什么，跟一个往外发的地址凑在一起才算；一句话前面有「不要 / 别 / 禁止 / 严禁」就不当回事——「不要用 `curl | bash`」是正经的安全提示，不是攻击。技能开头那段 frontmatter 里的提醒会升成拦下，因为 `description` 会被塞进**每一条**任务的系统提示词里，藏在那儿的一句话是全天候生效的。**它拦不住的那部分也说清楚**：同类静态扫描在公开数据集上只抓到约四分之三，四条里放过一条；而在本仓库自带的 34 个技能上，误拦率实测 5.9%（2 个，都是真命中的字面，不是假警报）——这个数字钉进了测试，以后改规则一旦误拦变多，CI 当场红

- **09-17** 技能和连接器的体检，可以多挂一把外面的尺子：本机装了 [toolward](https://github.com/CatCatUncle/toolward)（同一个作者的另一个项目，37 条规则分六族——提示词注入与工具投毒 TW1xx、供应链 TW2xx、密钥 TW3xx、执行与权限 TW4xx、网络与外传 TW5xx、治理 TW6xx），装技能和存连接器的时候就会顺手再扫一遍，两份结论合进同一张清单，每条都标着是谁说的。`.install.json`、强装记录、解释文案一个字没改——因为外挂的报告被做成了跟自带扫描器**一模一样的形状**，闸门、解释、落盘三处都不用动，只在三个装技能的入口各加一句 `merge()`。合的规矩是**只会更严不会更松**：它说拦就拦，它没说的按原来的判，所以挂上它不可能让本来拦得住的变成放行（这条有反向断言盯着）。**不进 `package.json`，也不 `npx`**：toolward 是 PolyForm Noncommercial 1.0.0 授权，个人、教学、学术、公益、政府免费，公司里用要单独授权（licensing@aijentra.com，可先试 30 天）——替所有商业用户默默添一条授权义务这种事不能做；而 `npx -y` 每次现拉一个没审过的版本，恰恰是它 TW2xx 那族要拦的行为，一边查供应链一边干这个说不过去。所以只认你自己 `npm i -g toolward` 装到 PATH 上的那一个（Electron 里 PATH 是残的，另外找了 `/opt/homebrew/bin`、`~/.volta/bin`、`~/.bun/bin` 这几个常见目录），也可以在设置里指死一个路径——指死了就不回退到 PATH，免得出现「我以为跑的是我自己编译那份」。**崩了不算拦**：超时（20 秒）、退出码不对、吐出来不是 JSON，一律返回「它没跑过」而不是「没扫出问题」——后者是句谎话，会让界面上写着「干净」。连着崩的还会歇 5 分钟，不然每装一个技能白等 20 秒。连接器那边只提醒、从不拦，而且**递过去的那份 `.mcp.json` 里没有你的 Key**：`env` 和 `headers` 的**值**全换成 `***`、键名留着（留着它才认得出「这儿有个 token 字段」，连键名一起换掉它反而会把每个配好的连接器都报成明文密钥），URL 只留到路径、问号后面剪掉，`command` 和 `args` 原样过去——那两个字段才是真会露出「npx 拉了个没锁版本的包」和「命令里串了个分号」的地方；测试里让假的 toolward 把收到的临时文件原样吐回来，逐字对过真 Key 没进去。整套东西有个 `OPENWORKBUDDY_TOOLWARD=off / advisory / auto` 的开关（环境变量压过设置项），测试默认关着：不然谁的机器上全局装了 toolward，skill-guard 那 76 条断言就会跟着变，绿不绿取决于开发机上装了什么

- **09-17** 成片能垫配乐了：画布上摆一个「声音」节点、素材用途写「配乐」，合成面板里就多一个「垫上配乐」的勾，用的是哪一首也写在勾旁边（画布上摆着好几段音乐时，混错了只能整条重跑）。一说话音乐自动压低（本机 ffmpeg 带 sidechaincompress 才有这一路，没有就固定音量垫着——开跑前就说，不是拼到最后一步才报错）；音乐比片子短就循环，比片子长也不会把片尾拉出几秒黑屏；台词一分贝都不会小：ffmpeg 的 `amix` 默认把每一路都除以 2，这是最容易悄没声把整条片子的对白弄小一半的地方，所以人声先乘回来、混完还过一道限幅
- **09-17** `wb` 这个简写在仓库里彻底退休了——上次改名只动了命令名，剩下的都还挂着。环境变量 `WB_*` → `OPENWORKBUDDY_*`（`WB_HOME` 是宿主机目录，跟容器里的 `OPENWORKBUDDY_HOME` 不是一回事，另起名叫 `OPENWORKBUDDY_DATA`），Docker 数据目录 `wb-data/` → `openworkbuddy-data/`，备份文件名 `wb-backup-*.tar.gz` → `openworkbuddy-backup-*.tar.gz`，登录 Cookie `wb_token` → `openworkbuddy_token`，浏览器本地的那些键和 CSS 变量 `wb-*` / `--wb-*` → `owb-*` / `--owb-*`。顺带治好一处对不上：交互模式的帮助里还印着 `wb>`，真提示符早就是 `openworkbuddy>` 了。**升级要做两件事**：Docker 部署的把目录改名 `mv wb-data openworkbuddy-data`、`.env` 里的变量名照 `deploy/env.example` 重抄一遍；所有人重新登录一次（Cookie 换名了）。不用手动搬的：侧边栏的任务历史会自己迁过去，老备份包照旧认得出来、还在列表里

- **09-17** 切换对话时输入框上头那根「上下文 87%」不再赖着不走。这根条全界面只有一根，而且只在任务跑起来、后端播事件的时候才重画；换到一条还没开跑的新对话既没有事件可等，也没人去收它，于是上一条那句「下一轮开跑前会自动压一次，想留全文就另开会话」原样挂着，指的却是别人家的数——新对话明明是空的，看上去却像一上来就快满了。现在三条换对话的路（点历史记录、跟一趟终端里起的任务、开新任务）都先把它收回去，本会话自己的数回放到了再填回来；顺带堵上另一半：后台并行跑着的那条会话，不再把它的余量画到你眼前这条对话上
- **09-17** 补上一道分公司之间的墙。按相对路径找文件时，兜底那一遍会把**所有记得住的工作目录**挨个扫一遍——包括别的分公司的，于是分公司的人光凭一个文件名、或者把别人那个目录的指纹抄进地址栏，就能把总部的文件读出来。现在候选目录先过一道租户过滤，租户外的一律不认；同一个租户内部跨子项目照旧找得回来，个人版（没有登录态，比如飞书回调和定时任务）一字不差走老路

- **09-17** 执行追踪里那一串一模一样的名字治好了。名字原来取的是**整段对话的第一句**，可一轮对话开一条追踪、传进来的又是整段历史——同一个会话聊十轮，列表上就是十行「OpenWorkBuddy日志技术选型」，点进去才知道是哪趟。现在取的是**最近那句有内容的话**，「继续」「好的」「嗯」「ok」这种接不上的短句自动往前找一句（「继续把日志接上」是正经需求，不算）；再给每条标上「第 N 轮」，名字撞车也分得开。**已经写进账本的老记录也一起分开了**：磁盘上的字不改，但每条都存着当时的完整输入，读的时候照新规矩重算一遍——只认「名字正好以这段历史第一句结尾、而后面还有别的话」这个旧规矩留下的指纹，自己起的名字碰不着，不会被乱改
- **09-17** 忘了密码、手机也丢了，不用等管理员了：在服务器本机敲 `openworkbuddy passwd <用户名>` 随机发一串新密码（也可以直接把新密码写在后面），`openworkbuddy 2fa <用户名>` 看二次验证开没开、`--off` 关掉。改完当场把这个人所有登录状态作废。**这两条只认本机命令行、绝不接到 HTTP 上**——凭的是「你能读到服务器上的 users.json」，本来就已经是最高权限，再开一个网络接口才是真送分
- **09-17** 入职和离职各收成一步，办完出一张回执。离职一次关完：账号停用、所有登录令牌作废、扫码配过的手机平板踢下线、他发出去还没用完的邀请码作废、二次验证解绑、名下的定时任务停掉并可以指名转给交接人（**转过去仍然是关着的**，让接手的人自己看过再开）；对话记录、产出的文件、用量流水和历史审计原样留着——人走了账还得能对。两道防手滑：不能给自己办，也不能把组织里最后一个在职管理员办掉（办完谁都进不了后台）。入职：按部门模板套好角色和月额度，一个部门招第三个人时不用再去回忆前两个是怎么配的
- **09-17** 出了事能看见了：运行日志按天落盘、自动轮转不撑爆磁盘（敏感字段照样脱敏，Key 不会顺着日志漏出去），关键指标每分钟滚一份快照（请求量、耗时分位、失败率、各渠道花了多少），设置 → 运行状况一页看完，越线自动推到企业微信 / 钉钉。同一条规则 30 分钟内只报一次，**恢复了也说一声**——只报警不报恢复的系统，用不了两周大家就开始无视它。分位数改成了「最近邻」算法——样本少的时候，插值法算出来的 P95 是一个**从来没真实发生过**的数，看着比实际好
- **09-17** 用量账本拆成按月分片，顺带修掉一处**会让账悄悄少算**的老设计：原来整本账是一个 `usage.json`，每记一笔要整本读出来、插一条、再整本写回去，而且只留最新 2000 条——50 个人每天各用 10 次，**四天就撞顶**，后台的「本月用量」「谁在用」从此少算，还一个错都不报。现在一笔一行直接追加到 `data/usage/2026-09.jsonl`，追加耗时跟历史多长完全无关（老写法 10 万条要 262ms，每趟任务结束都付这笔钱），查「本月」只读一个分片，也不再封顶——被封顶丢掉的正是计费和审计数据。老的 `usage.json` 第一次用到账本时自动拆开，**不删**，改名成 `.migrated` 留在原地好对账。执行追踪那本也一起接上了轮转（最多留 6 本，约 144 MB 封顶）
- **09-17** 远程那两条路各给了一个开关，**默认都是关的**：「允许扫码接入远程设备」（手机、另一台电脑）和「允许从网页/手机操控终端里正在跑的任务」。装完就默默开着的话，人不一定知道自己刚把什么暴露到了局域网上。开关在企业设置里，**关掉的同时已经配过的设备下一次请求就被踢下线**——不踢的话这个开关就是假的：你以为手机断了，其实没断
- **09-17** 预览里的图能复制了：预览面板、无限画布的大图、资料库里点开的图，`Ctrl/⌘+C` 直接进剪贴板，粘到微信、PPT、飞书里都是图本身而不是一条本机路径；桌面版右键也多了「复制图片」「复制图片地址」「另存为」
- **09-17** 企业设置那条路由是把请求体里的 `settings` 整个交进来的，所以新加的表结构设置（部门模板、额度表）落盘前一律先拍干净：只认清单里的字段、角色只认 admin/auditor/member、数字夹进范围。不拍的话，认了这个键就等于认了请求体能往组织设置里写一张任意结构的表——里头塞个越权的角色，办入职时就照着建号了；顺带修掉一处会把整张表存成 `"[object Object]"` 的老写法
- **09-17** 命令行改名 `wb` → **`openworkbuddy`**。理由不是好听，是防混淆：`wb` 太短，容易被读成别家产品的缩写。`npm link` / 全局安装过的重装一次（`npm unlink && npm link`），补全脚本重新生成一份（`openworkbuddy completion zsh > ~/.zsh/completions/_openworkbuddy`），老的 `_wb`、`~/.wb-completion.bash` 删掉。选项和子命令一个没动，只是命令名换了
- **09-17** 密码这块补齐了三样：**密码策略**（企业设置里改最短位数 6-64、可选复杂度要求；注册、自己改密码、管理员重置三个入口一起管，系统生成的随机密码也跟着变长）、**二次验证**（TOTP，Google Authenticator / 1Password 都能扫，可全组织强制）、**忘记密码自助**（在跑着服务的那台机器上敲一句 `openworkbuddy passwd <用户名>` 就改回来了，不依赖邮件服务器；手机丢了进不去二次验证的还有 `openworkbuddy 2fa <用户名> --off`。登录页上就写着这两句话该去哪儿敲，不用翻文档）
- **09-17** 授权这件事说清楚了：**买商业授权不解锁任何功能**——只有一份代码，装的这份就是全部。买到的是三样：一张允许你拿它赚钱的许可证、商标与白标的口子、能找到人的支持。以前文档把「SSO、审计外送、内网部署」写得像现货，其实一个都还没写，现在明确标成路线图
- **09-17** 开了一个口子：部署配置、CI 流水线、`scripts/`、评测任务集与基线、技能模板 `skills/skill-creator/`、文档里的示例代码与格式规范，**额外按 MIT 发布**，商用也随便拿（[LICENSE-ECOSYSTEM.md](LICENSE-ECOSYSTEM.md)）。同时写明：你自己写的技能、插件、连接器配置是你自己的作品，不是本项目的衍生作品
- **09-17** 把仓库整个盘了一遍「还有哪些能开源」：代码这侧已经没有可放的了，没公开的只剩第三方技能包（别人的授权）、来源不明的技能、带私有凭据的公众号管线、用户数据四类，每类的理由写进了[边界文档](docs/开源与商业版边界.md)第 3 节；管理后台侧栏那个「企业版」徽章改成「管理后台」——它是页面名字，不该被读成档位
- **09-17** 资料库里的预览修好了：图片、Markdown、HTML、PDF 点开就能看；「名字还在、东西不在」会直说是哪一种（被挪走了还是读不出来），不再是一个碎图标、一片空白或者一句「读取失败」；清单里查不到体积的写「—」，不再撞下限撞出一个「1 KB」的假数
- **09-17** 桌面版点「新窗口打开」不再甩 `{"error":"未登录","setup":false}`：站内地址留在应用里开一个子窗口，登录 cookie 跟着走；只有真外链才交给系统浏览器——「本地部署预览」那条不在此列，它是另一个进程另一个端口、链接自带令牌，本来就该去浏览器（手机扫码也靠它）
- **09-17** 终端里它也会反问你一句了：岔路口（报告交 Word 还是 PDF）摆一道选择题，每个选项底下写清楚选它意味着什么；序号、选项名、名字的一小段都认，也可以不选、直接把想法打进去。**只有真有人坐在终端前才会问**——管道、`--json`、cron 里跑的那些，agent 收到的是「没人在线，按最合理的默认继续做」，不会挂在那儿等一个永远不来的回答
- **09-17** `openworkbuddy` 大修：`sessions` / `resume` / `engines` / `doctor` / `completion` 五个子命令（老选项一个没动），`-f` 带文件图片一起问、`--raw` 出原始 Markdown、`-V` 看版本；退出码分得开（`2` 是参数写错、`130` 是 Ctrl+C），认 `NO_COLOR` / `FORCE_COLOR`；三种 shell 的 Tab 补全跟 `--help` 长在同一张表上，加一个选项三处同时就有
- **09-17** 交互模式里打一个 `/` 就把命令菜单弹出来——记不住命令的人第一反应是打个 `/` 等着看有什么，不会去按 Tab；只弹不选，没按过 ↑↓ 或 Tab 就按你原样发走，终端不认那些控制序列就整场关掉菜单退回 Tab 补全，绝不把输入行搅成一团
- **09-17** 粘 Key 时多框进来的空格、换行、中文引号，保存那一刻就指出是第几个字符不对，不用等发出去收一个看不懂的 401；对话和生图 / 配音 / 生视频两路现在过的是同一道闸
- **09-17** `config.json` 落盘权限收成 0600：以前是 umask 给的 0644，同一台 VPS、同一台办公电脑上任何一个别的账号一句 `cat` 就把所有 Key 拿走；开机先收一次，把装了半年的老机器一并修好，`.bak` 跟正本一样收
- **09-17** 权限这一圈收完了：账号库 `users.json`、组织库 `orgs.json` 跟 `config.json` 一样 0600（里头是密码哈希、二次验证的种子和还没用掉的邀请码——同一台机器上别的账号 `cat` 一下，拿到的是一份能直接拿去登录的材料），`data/` 和 `logs/` 两个目录收成 0700。日志这条尤其值得单说：一行一条 JSON，带着谁在什么时候登录了、会话 id、报错原文和出错时的那几个参数，默认 0755 的话它就是一份**不留痕迹**的「谁在干什么」流水。目录是出生时就带着 0700 的，不是建完再改——中间那一瞬的 0755 也不留。Windows 上 chmod 基本是空操作，这条只在 macOS / Linux 上算数
- **09-17** 换 Key 进审计流水了：多管理员的组织里，谁把哪条渠道从哪把换成了哪把，事后查得到（两头都只记「前三位…末四位」，明文不进审计表）
- **09-17** 生视频认五家协议（通义万相 / 火山方舟 Seedance / 智谱 CogVideoX / MiniMax 海螺 / 硅基流动）：先看渠道类型、再看地址域名、还能在 `config.json` 上逐条写 `protocol`，三条都认不出**就不发那一趟**——按条计费的异步任务白跑一趟要等好几分钟才看见错；不收尾帧的渠道传了 `last_frame` 也在发请求前拦下；手写的 `protocol` 不会再被设置页保存时抹掉；只做视频的渠道不再出现在对话模型的选单里
- **09-17** 回答正文里 `[调研报告](报告.md)` 这类指向工作区文件的链接点得开了（以前只认 `https:`，屏幕上原样印出一串方括号圆括号），键盘回车也认；认不准的一律不给链接，`javascript:` 和往地址里塞引号都进不了属性
- **09-17** 首页场景按「你要交出什么」重排，最后收成**三类**（文档与汇报 / 数据与内容 / 代码与设计）——分到五类以上，人就得先在脑子里给自己的活归个类才敢点，那一步本来就是多余的；同一件事不再两个入口、也不再有「金融服务」这种没法交办的行业名；无限画布挪到侧栏「项目」上面——项目底下挂着一列会长的子项，排在它后面的看着像「项目的最后一条」
- **09-17** 自进化修了一处会把规则吞掉的顺序错：超字数预算时砍掉的原来永远是**最新**那几条——人刚在评审页点了「采纳」、界面写着「已生效」，模型一个字都没收到，还不吭声。现在保新砍旧、点名砍了哪几条，尾巴那句「另有 N 条」也算进预算里
- **09-17** 提示词把「什么该问、什么自己定」收成三类（白做的关键信息 / 成品形态岔路 / 花钱不可逆和只有你知道的事），技术路线永远算自己定；音频视频一律先 `transcribe_audio` 转文字再动手，不许 `read_file` 读二进制瞎猜
- **09-17** 换电脑搬家闭环：设置 → 数据里能「立即备份」「下载」「导入备份文件」「恢复」，备份覆盖账号、组织、会话、IM 会话、记忆、个人偏好、用量账本、审计、`config.json`、定时任务、专家和自己写的技能；导入的包落盘前整个拆开校验，绝对路径、`..`、软硬链接、非 gzip 和顶层多余目录一律拒掉，恢复前自动再备一份 `before-restore`
- **09-17** 任务执行中再发消息可以自己选插队还是排队：插队打断当前步骤当场改，排队等这轮跑完再按顺序开；排队里的那条能撤回，这个选择记在本机、不跟着账号走
- **09-17** 按次收费的接口（搜索、生图、视频、语音、转写）加了额度闸门：超额前先问、记下谁花的和走的哪个渠道，企业后台按天、按月、按人翻账；默认不限额，自己带 Key 的个人用户不受影响
- **09-17** 企业管理后台的用量可以按时间范围、按成员、按模型筛选并翻页，不再只有最近 25 条
- **09-16** 飞书渠道补齐群聊与云文档评论闭环：群聊中 @ 机器人可执行任务并回传文字/文件；飞书云文档评论区 @ 机器人会识别目标文档、读取正文上下文，并在原评论线程回复；修正评论回复接口参数位置，避免 `field validation failed`；文档正文与用户评论分离传给 Agent，避免长文档被原样回显；去重状态跨重启保留
- **09-15** README 改成“先看到真实交付、再用一句话上手”的项目首页：首屏讲清本机文件交付，紧跟可复制的第一次任务和 Star/贡献入口；补齐英语用户的本地办公、自托管与 Agent 学习定位，新增 `llms.txt` 让搜索和 AI 摘要更易正确理解项目；同时补齐 DAG 排版依赖的 MIT 署名，完整测试重新全绿
- **09-15** 无限画布交互收口：空白处左拖平移、`Shift`+拖拽框选、框选后拖任一节点整体移动；节点正文只选中不弹属性，只有齿轮才打开设置，避免操作时遮住画布；顶部间距收紧，把更多高度还给画布
- **09-15** README 首屏改为脱敏的真实任务演示（输入 → 执行 → 文件预览），AI 短剧无限画布移到功能介绍之后，新增脱敏 DAG 效果图和独立录屏；录制脚本不会再把画布录屏覆盖到 README 首屏

- **09-14** AI 短剧无限画布进入可用版：画布 Agent、DAG 连线与紧凑排版、框选/撤销/右键操作、节点属性与素材预览、图片放大、视频/音频播放、工作区切换、@ 引用、模型选择和本地 Trace 全部收进同一个短剧工程；README 新增真实运行、完全脱敏的无限画布录屏 GIF，方便贡献者快速理解产品
- **09-14** 增加 AI 短剧无限画布模式：剧本、角色、场景、分镜、素材、生成结果和本项目 Agent 在同一张可执行画布内协作，支持多张短剧工程、素材拖入、框选批量操作、自动排版和画布内对话
- **09-14** 优化无限画布短剧创作 UI：参考图/视频/音频可预览，图片支持放大查看，画布对话支持附件、`@` 引用节点与素材、执行模式和模型选择
- **09-14** 飞书消息去重跨重启持久化：已处理的消息 ID 写入本地数据文件，重启后不再重复执行旧任务；文件只保存消息 ID，不保存消息正文或联系人信息
- **09-13** 讲故事的片子能拍了：先把分镜表摆出来给你点头，再角色定妆照 → 每镜首帧 → 生视频 → 配音 → 拼成带字幕的竖版片；改哪一镜就只有那一镜再花钱
- **09-13** 同一格重跑不再花第二次钱：生图 / 生视频 / 配音的参数跟上一次逐字一样时，直接复用上次的产物，连模型都不调（确实要换一版，工具参数加 `no_cache: true`）；另开了一条不过模型的直调口 `POST /api/tool/run`，「同样的参数原样再跑一遍」这种确定性动作不用再烧一轮主模型的 token 去把提示词复述给它听，它也没机会顺手改写你那句话——能这么直调的只有生图 / 生视频 / 配音 / 网页截图这四个
- **09-13** 企业后台的渠道表能自己加、能改、能删了：自建网关、公司内网代理、换了域名的私有部署这些预置目录里没有的，点「新渠道」当场填；同一个地址挂了两条会标出来而不是并成一条（各是一个账号一把 Key、各花各的额度），删渠道之前先把连坐的数报清楚——底下挂着几个对话模型、几个媒体模型、默认模型在不在里面
- **09-13** 命令跑不起来时不再只甩一句 `command not found`：认得出的外部工具会直接说清缺的是哪个、它管什么、这台机器上（mac / Windows / Linux 各不一样）该敲哪一句装；`ffprobe`、`libreoffice` 这类别名会折回它所属的那个包，不会让人去搜一个根本不存在的东西，认不出的命令则一个字都不加
- **09-13** 「任务最大运行时间」的默认值四处对不上——配置模板写 10 分钟、代码按 30 分钟跑、设置页文案又写「默认 10」——现在统一成 30 分钟，并把「自动续跑轮数」补进配置模板（默认仍是 0，因为每一轮都真花钱）；图文成片技能也多了一条前置提醒：分镜超过 6 段先去把步数和时长调上去，别让片子断在最后一步拼装之前——那时候图和配音的钱都已经花完了
- **09-13** 出图、出片、配音现在能在同一轮里一起跑，默认同时 2 条（设置里 1-4 可调）：一集短剧十二个镜头不用再一条条排，但每条都花钱所以给得比搜索那档保守；顺带修掉没起名的产出在同一毫秒里会写成同一个文件名、后一张盖掉前一张还两张都报成功的坑
- **09-13** 生图能喂参考图（最多 4 张），生视频能定首帧、尾帧：同一个人同一件商品跨镜头不再长变样；文生/图生型号选反了在发请求之前就拦下来，渠道真不认也明确报错，不偷偷退回纯文生
- **09-13** 会议纪要师、合同审阅员、邮件沟通师、项目经理、产品经理、招聘专员、客服话术师这七位现在各带一本写死的作业规范，交出来的东西有固定结构、有红线，不再是每次一个样
- **09-13** 配一条 SMTP，AI 就能把写好的报告、做好的文件直接发到对方邮箱；收件人白名单是硬闸，每封信出门前还要你看全文点头
- **09-13** AI 能自己排定时任务了：「每周一早九点把上周数据整理成表」说一句就定下，到点自己跑
- **09-13** 录音、会议视频、语音留言直接转文字，转完接着往下写纪要，不用自己再听一遍
- **09-13** 甲方发来的 Word / Excel / PPT / PDF 直接读，资料库里的素材随手取，做完的东西一句话推到群里
- **09-13** 终端里也看得到产出的图了：`/open` 交给系统程序打开，认得出的终端直接画在对话里
- **09-13** 发版这条链补齐：PR 有红绿灯、版本号对不上当场红、单平台挂了不拖垮整次发版
- **09-13** 做网页不再千篇一律：动笔前先挑视觉方向，同一个需求两次做出来不一样
- **09-13** 每一步花了多久直接写在卡上，收尾再记一笔账：工具占了多久、剩下多少是模型在想
- **09-13** 接 Langfuse 就能翻每次模型调用的原始输入输出，一层层点开看
- **09-13** API Key 五连坑一起修：填了不认、每次弹向导、渠道重名、没地方填、模型不齐
- **09-13** 企业管理后台补「模型与 Key」页，管理员在后台就能配，不用回首页
- **09-11** 侧栏分「办公 / 工程」两条线；终端里 `openworkbuddy` 起的活儿，手机上看得见、插得上话
- **09-11** 生图 / 生视频 / 配音 / 视觉都能配多个模型，同一家的 Key 只填一次
- **09-11** 「下载了打不开」修了六种，每种都有窗口说清原因，另附一份启动日志
- **09-11** 装机包瘦了 139 MB：source map 和类型声明运行时一个字节都不读
- **09-11** 「检查更新」不再劝装了包的人去 `git pull`
- **09-11** 补三处高危：改一个字母绕过门禁、文件名能执行命令、预览站点对全网敞开
- **09-11** 多人服务器上，普通成员不再看见一排点了必然 403 的按钮
- **09-11** UI 精致化：三级表面层级、统一投影、修掉不可见的滚动条
- **09-10** 任务历史不是丢了，是被一个凭空编出来的项目名滤没了——已修
- **09-10** 一条命令部署到服务器：`bash deploy.sh`，加 `--domain` 直接带 HTTPS
- **09-10** 多租户 + 企业管理后台：16 个面板，管理员、审计员、成员各看各的
- **09-10** 不许「没做完就收摊」：收尾前对一遍进度档，还有没打勾的就打回去
- **09-10** 执行过程一行流：每步一行，原始入参和完整返回收在卡里点开就是
- **09-10** 长回复不卡了：十万字流式，DOM 重建 19.7 秒 → 0.7 秒
- **09-10** Goal 目标卡：拆验收、判分都走你本机 CLI，不偷偷烧 API 额度
- **09-10** 微信 / 企微 / 公众号 / QQ 也能收文件、图片、语音、表情了
- **09-10** 飞书扫码新建应用，App ID 自动填回
- **09-09** 任务完成不再弹面板抢版面，产出收成一排 chip
- **09-09** 连接器 39 个预设一键接入；专家加到 15 位 + 4 支专家团
- **09-09** 中英文一键切换；外观页给了主题、六套皮肤、字号和密度
- **09-08** 首次开箱五步向导，打开就带你配好模型、引擎、IM
- **09-08** 本机 Claude Code / Codex 当引擎，记忆、技能、读文件、生图配音全接上
- **09-08** 带 reasoning 的模型可以关掉思考或调强度
- **09-07** 定时任务加「假绿」裁定：没抛异常不等于干成了
- **09-05** 命令行 `openworkbuddy`：单发 / 交互 / 管道 / `--json`，退出码说实话
- **09-03** 应用内直接预览 docx / xlsx / pptx / zip / csv
- **08-31** 粘贴、拖拽文件和图片进对话，agent 真能看懂图

---

[← README](README.md)
